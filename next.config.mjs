
// next.config.mjs
const nextConfig = {
    images: {
      domains: ['cdn.vectorstock.com', 'api.nakshatranamahacreations.in'], // ✅ Add the external hostname
    },
  };
  
  export default nextConfig;
  